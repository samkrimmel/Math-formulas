#Sam Krimmel
#1/16/18
#equationofquadratic.py - gives the equation of a quadratic given the vertex and a point

xv = float(input('x-coordinate of vertex = '))
yv = float(input('y-coordinate of vertex = '))

x1 = float(input('X1 = '))
y1 = float(input('Y1 = '))

print('ax^2+bx+c=y')

b = (yv-y1)/((xv/2)+(((x1**2)/(2*xv))-x1))
a = ((-b)/(2*xv))
c = (y1+(b*(x1**2))/(2*xv)-(b*x1))

print('a =', a)
print('b =', b)
print('c =', c)
